# International Delivery (skeleton)

Repository skeleton for International Delivery microservice.

Steps to run:
1. Configure `.env` based on `config/.env.example`
2. poetry install
3. docker-compose up
